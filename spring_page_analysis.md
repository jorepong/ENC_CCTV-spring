# Spring 웹 애플리케이션 - 분석 페이지 문서

## 1. 페이지 개요

분석 페이지는 선택된 단일 카메라에 대한 심층적인 데이터 분석을 제공하는 페이지입니다. 실시간 영상, AI 분석 스냅샷, 시계열 통계 차트, 혼잡도 히트맵, 이상 패턴 감지, 추세 분석 등 다양한 분석 도구를 통합하여 제공합니다.

**접근 경로**: `/analysis?cameraId={id}`  
**컨트롤러**: `WebController.analysisPage()`  
**템플릿**: `analysis.html`  
**JavaScript**: `analysis.js`

## 2. 페이지 구성 요소

### 2.1. 헤더 영역

#### 2.1.1. 페이지 제목 및 상태
- 브레드크럼: "관제 > 상세 분석"
- 선택된 카메라 이름을 큰 제목으로 표시
- 현재 혼잡도 상태 칩 (여유/주의/위험/데이터 없음)을 제목 옆에 표시
- 카메라 위치 정보 (address 또는 locationZone)를 부제목으로 표시

#### 2.1.2. 카메라 선택 드롭다운
- 모든 등록된 카메라 목록을 드롭다운으로 제공
- 카메라 선택 시 `window.location.href = '/analysis?cameraId=' + this.value`로 페이지 새로고침
- 현재 선택된 카메라는 `selected` 속성으로 표시
- 카메라가 없는 경우 "사용 가능한 카메라 없음" 메시지와 함께 비활성화

### 2.2. 메인 콘텐츠 영역 (왼쪽 스택)

#### 2.2.1. 실시간 영상 카드

**YouTube Live 스트림 표시**
- `streamType`이 `YOUTUBE`이고 `streamUrl`이 있는 경우 iframe으로 임베드
- YouTube URL을 임베드 형식으로 변환: `watch?v=` → `embed/`
- 자동재생, 음소거, 컨트롤 숨김 등의 파라미터 추가:
  - `autoplay=1`: 자동 재생
  - `mute=1`: 음소거
  - `controls=0`: 컨트롤 숨김
  - `showinfo=0`: 정보 숨김
  - `modestbranding=1`: YouTube 로고 최소화
  - `disablekb=1`: 키보드 컨트롤 비활성화
  - `rel=0`: 관련 동영상 표시 안 함
- iframe은 부모 컨테이너를 가득 채우도록 absolute 포지셔닝

**스트림 없음 처리**
- `streamType`이 `YOUTUBE`가 아니거나 `streamUrl`이 없는 경우
- "실시간 스트림을 표시할 수 없습니다." 메시지 표시

#### 2.2.2. 최신 분석 스냅샷 카드

**스냅샷 이미지 표시**
- Django 서버가 30초마다 캡처하여 AI 분석한 최신 이미지 표시
- 이미지 경로는 서버에서 렌더링 시 `latestAnnotatedImagePath`로 전달
- 경로는 `/media/` 접두사가 붙은 웹 접근 가능 경로로 변환됨
- 스냅샷이 없는 경우 "분석 스냅샷이 없습니다." 플레이스홀더 표시

#### 2.2.3. 시계열 통계 분석 카드

이 카드는 분석 페이지의 핵심 기능으로, 밀집도의 시간에 따른 변화를 시각화하고 과거 데이터와 비교할 수 있는 인터랙티브 차트를 제공합니다.

**기간 선택 컨트롤**
- 3개의 기간 옵션 제공:
  - **2시간**: 최근 2시간 데이터 (기본값)
  - **24시간**: 최근 24시간 데이터
  - **1주일**: 최근 7일 데이터
- 선택된 버튼은 `is-active` 클래스로 강조 표시
- 클릭 시 `activePeriod` 상태 변경 및 차트 갱신

**비교 선택 컨트롤**
- 2개의 비교 옵션 제공:
  - **어제**: 선택한 기간의 어제 동시간대 데이터와 비교
  - **지난주**: 선택한 기간의 지난주 동시간대 데이터와 비교
- 비교 옵션은 토글 방식으로 작동 (재클릭 시 비교 해제)
- 선택된 버튼은 `is-active` 클래스로 강조 표시
- 클릭 시 `activeCompare` 상태 변경 및 차트 갱신

**상호 배타적 제약**
- "어제" 비교 선택 시 "1주일" 기간 버튼 비활성화
  - 이유: 1주일 기간에 어제 데이터를 비교하는 것은 의미가 없음
- "1주일" 기간 선택 시 "어제" 비교 버튼 비활성화
  - 이유: 7일 기간에 24시간 전 데이터를 비교하는 것은 시각적으로 혼란스러움

**차트 데이터 로딩 및 처리**

1. **데이터 페칭**
   - API: `GET /api/v1/cameras/{cameraId}/density-history?start={start}&end={end}`
   - 주 데이터: 선택한 기간의 밀집도 데이터
   - 비교 데이터 (선택 시): 과거 동일 기간의 밀집도 데이터
   - 두 데이터를 병렬로 `Promise.all()`로 페칭하여 성능 최적화

2. **데이터 리샘플링**
   - 목적: 불규칙한 시간 간격의 데이터를 고정된 시간 버킷으로 정규화
   - 버킷 크기:
     - 7일 기간: 1시간 간격
     - 24시간 기간: 5분 간격
     - 2시간 기간: 5분 간격
   - 각 버킷에 대해 계산:
     - `avg`: 해당 시간 구간의 평균 밀집도
     - `min`: 최소 밀집도
     - `max`: 최대 밀집도
     - `maxLog`: 최대 밀집도를 기록한 원본 로그 (스냅샷 이미지 포함)

3. **비교 데이터 시간 이동**
   - 비교 데이터의 타임스탬프를 주 데이터의 시간대로 이동
   - 어제 비교: +24시간 이동
   - 지난주 비교: +7일 이동
   - 이를 통해 X축을 공유하면서 두 데이터를 겹쳐서 표시 가능

**차트 렌더링 (Chart.js)**

차트는 4개의 데이터셋으로 구성됩니다:

1. **Range Min** (숨김 데이터셋)
   - 각 시간 구간의 최소 밀집도
   - 얇은 파란색 선 (`rgba(37, 99, 235, 0.3)`)
   - 범위 영역의 하한선 역할

2. **변동 범위** (Range Max)
   - 각 시간 구간의 최대 밀집도
   - 얇은 파란색 선 (`rgba(37, 99, 235, 0.3)`)
   - Range Min까지 채워져 변동 범위를 시각화 (`fill: '-1'`)
   - 반투명 파란색 배경 (`rgba(37, 99, 235, 0.25)`)

3. **평균 밀집도** (주 데이터)
   - 각 시간 구간의 평균 밀집도
   - 진한 파란색 선 (`#1d4ed8`)
   - 두께 2.5px로 강조
   - 레이블: "현재" / "오늘" / "이번 주" (선택한 기간에 따라)

4. **비교 데이터** (선택 시)
   - 과거 동일 기간의 평균 밀집도
   - 회색 점선 (`#94a3b8`, `borderDash: [5, 5]`)
   - 두께 2px
   - 레이블: "어제" / "지난 주"

**차트 스케일링**
- Y축: 0부터 시작
- Y축 최대값: 데이터 최대값의 120% 또는 0.5 중 큰 값
- X축: 자동 간격 조정, 최대 10개 눈금

**인터랙티브 툴팁**

툴팁은 커스텀 HTML 요소로 구현되어 풍부한 정보를 제공합니다:

**툴팁 표시 내용**
- 시간 레이블 (예: "8일 14:30")
- **분석 스냅샷 이미지**: 해당 시간 구간에서 최대 밀집도를 기록한 순간의 AI 분석 이미지
- 최대 밀집도 값 (강조 표시)
- 평균 밀집도 값
- 최소 밀집도 값
- 최대 인원 수 (해당 로그의 `personCount`)
- ROI 수용량 계산: `personCount / maxDensity` (자동 계산되어 표시)

**툴팁 고정 기능**
- 차트 위에 마우스를 올리면 툴팁이 표시됩니다.
- 차트를 클릭하면 툴팁이 고정되어 마우스를 떼어도 유지됩니다.
- 고정된 툴팁은 파란색 테두리와 그림자로 시각적으로 구분됩니다.
- 고정 해제 방법:
  - 차트를 다시 클릭
  - ESC 키 누르기
  - 차트 외부 영역 클릭
- 고정된 툴팁은 `pointerEvents: 'auto'`로 설정되어 마우스 인터랙션 가능

**플레이스홀더 처리**
- 데이터 로딩 중: "분석 데이터를 불러오는 중입니다..."
- 데이터 없음: "해당 기간에 대한 분석 데이터가 없습니다."
- 로딩 실패: "차트 데이터를 불러오는 데 실패했습니다."

#### 2.2.4. 요일-시간대별 혼잡도 히트맵 카드

히트맵은 지난 7일간의 데이터를 요일과 시간대별로 집계하여 패턴을 시각화합니다.

**히트맵 구조**
- Y축: 요일 (월~일)
- X축: 시간 (0~23시)
- 총 7 × 24 = 168개의 셀로 구성
- 각 셀은 해당 요일의 해당 시간대의 밀집도를 색상으로 표현

**데이터 소스**
- API: `GET /api/v1/cameras/{cameraId}/congestion-heatmap`
- 응답: `List<CongestionHeatmapPayload>`
- 각 요일에 대해:
  - `dayOfWeek`: 요일 이름 (예: "월")
  - `dayOfWeekIndex`: 요일 인덱스 (1=월요일, 7=일요일)
  - `hourlyAverageDensities`: 시간별 평균 밀집도 배열 (24개)
  - `hourlyMaxDensities`: 시간별 최대 밀집도 배열 (24개)

**색상 매핑 알고리즘**

히트맵의 색상은 밀집도 값을 HSL 색상 공간으로 변환하여 생성합니다. 이 알고리즘은 낮은 밀집도에서 높은 밀집도까지 직관적인 색상 변화를 제공하도록 설계되었습니다.

1. **정규화**: 밀집도를 0~1.2 범위로 정규화
2. **색상 구간 분할**:
   - **0~0.4 (낮은 밀집도)**: 파랑(210°) → 청록 → 초록(110°)
     - S-커브 함수 적용하여 부드러운 전환
   - **0.4~1.0 (중간~높은 밀집도)**: 초록(110°) → 노랑 → 주황 → 빨강(0°)
     - 선형 전환
3. **채도 조정**:
   - 기본: 60% → 95% (밀집도에 비례)
   - 녹색 구간(100°~150°): 채도 35% 감소하여 덜 쨍하게 표현
4. **명도 조정**: 70% → 35% (밀집도에 비례하여 어두워짐)
5. **특수 처리**:
   - 밀집도 0 이하: 연한 회색 (`#ebedf0`)

**색상 예시**
- 0.0: 연한 회색
- 0.2: 밝은 파란색
- 0.4: 청록색
- 0.6: 노란색
- 0.8: 주황색
- 1.0 이상: 빨간색

**히트맵 툴팁**
- 셀 위에 마우스를 올리면 커스텀 툴팁 표시
- 툴팁 내용:
  - 요일 및 시간 (예: "월요일 14시")
  - 최대 밀집도 (소수점 3자리)
  - 평균 밀집도 (소수점 3자리)
- 툴팁은 마우스 커서를 따라 이동
- 브라우저 기본 툴팁 방지를 위해 `title` 속성 제거

**색상 범례**
- 히트맵 하단에 그라데이션 바 표시
- "낮음" ~ "높음" 레이블

### 2.3. 사이드바 영역 (오른쪽 스택)

#### 2.3.1. 종합 분석 카드

**게이지 차트**
- SVG 원형 게이지로 현재 밀집도를 시각화
- 외곽 원: 연한 파란색 배경 (`#eef2ff`)
- 내부 원: 혼잡도 수준에 따라 색상 변경
  - 위험: 빨간색 (`#ef4444`)
  - 주의: 주황색 (`#f97316`)
  - 여유: 파란색 (`#2563eb`)
- 원의 채워진 정도: `gaugeDensity` 값 (0~1)에 비례
  - `stroke-dasharray`를 사용하여 원주의 일부만 그리기
  - 계산: `gaugeDensity * (2 * π * 48)` (반지름 48px 기준)
- 중앙 텍스트: 밀집도 백분율 (예: "45%")
- 하단 레이블: 혼잡도 수준 (여유/주의/위험)

**현재 밀집도 (Raw)**
- 원시 밀집도 값을 소수점 3자리까지 표시
- 이 값은 Django 서버에서 계산한 정규화되지 않은 밀집도

**위험 구간 지속 시간**
- 현재 밀집도가 위험 임계값(1.0) 이상인 경우에만 표시
- 위험 상태가 시작된 시각부터 현재까지의 경과 시간
- "HH:MM:SS" 형식으로 표시
- `CameraAnalyticsSummary.timeInDangerFormatted()`에서 계산

**전일/지난주 동시간 대비**
- 현재 시각 기준 ±7분 윈도우의 평균 밀집도를 과거와 비교
- 전일 동시간 대비:
  - 24시간 전 동일 시간대의 평균 밀집도와 비교
  - 변화량을 백분율로 표시 (예: "▲ 15.3%" 또는 "▼ 8.2%")
  - 증가: 빨간색 상향 화살표
  - 감소: 초록색 하향 화살표
  - 변화 없음: 회색 "- 0%"
- 지난주 동시간 대비:
  - 7일 전 동일 시간대의 평균 밀집도와 비교
  - 동일한 형식으로 표시
- 데이터 없음: "데이터 없음" 메시지 표시

**데이터 로딩**
- API: `GET /api/v1/cameras/{cameraId}/comparison-summary`
- 페이지 로드 시 한 번 호출
- 응답:
  - `yesterdayDensity`: 어제 동시간 평균 밀집도
  - `yesterdayChange`: 변화량 (현재 - 어제)
  - `lastWeekDensity`: 지난주 동시간 평균 밀집도
  - `lastWeekChange`: 변화량 (현재 - 지난주)

#### 2.3.2. 이상 패턴 감지 카드

이 카드는 통계적 방법을 사용하여 현재 밀집도가 과거 패턴과 비교하여 이상한지 판단합니다.

**통계적 이상 감지 원리**
- 과거 4주간의 동일 요일, 동일 시간대(±1시간) 데이터를 수집
- 평균(μ)과 표준편차(σ)를 계산
- Z-점수 계산: `z = (현재값 - μ) / σ`
- Z-점수 해석:
  - `z > 2.5`: 이례적으로 높음 (위험)
  - `z > 1.5`: 평소보다 높음 (주의)
  - `z < -1.5`: 평소보다 낮음
  - 그 외: 정상 범위

**시각화**
- 그래프 영역: μ ± 3σ 범위를 100% 너비로 표시
- 정상 범위 바: μ ± σ 구간을 회색 바로 표시
- 현재 값 마커: 파란색 수직선으로 현재 밀집도 위치 표시
- 이상 상태 시 마커가 빨간색으로 변경 (`is-anomaly` 클래스)

**상태 칩**
- 메시지 표시: "이례적으로 높음", "평소보다 높음", "정상 범위" 등
- 색상:
  - 위험 (z > 2.5): 빨간색
  - 주의 (z > 1.5): 주황색
  - 정상: 회색

**상세 정보**
- 현재 값 및 정상 범위 (μ - σ ~ μ + σ)
- 평균으로부터의 편차 (예: "평균(0.45)으로부터 2.3 표준편차")

**분석 불가 상태**
- 데이터 부족 (20개 미만): "과거 데이터 부족 (X개)"
- 변동 없음 (σ < 1e-6): "변동 없음"
- 이 경우 그래프 숨김, 메시지만 표시

**데이터 로딩**
- API: `GET /api/v1/cameras/{cameraId}/statistical-anomaly`
- 페이지 로드 시 한 번 호출
- 응답:
  - `isAnalyzable`: 분석 가능 여부
  - `message`: 상태 메시지
  - `currentDensity`: 현재 밀집도
  - `averageDensity`: 과거 평균
  - `stdDeviation`: 표준편차
  - `zScore`: Z-점수

#### 2.3.3. 추세 분석 카드

**밀집도 변화율 (속도)**
- Django에서 계산한 밀집도의 1차 미분값
- 단위: `/min` (분당 변화량)
- 소수점 4자리까지 표시
- 배지로 추세 표시:
  - `> 0.05`: "빠른 증가" (빨간색)
  - `> 0.005`: "증가세" (주황색)
  - `< -0.05`: "빠른 감소" (초록색)
  - `< -0.005`: "감소세" (초록색)
  - 그 외: "안정" (회색)

**변화 가속도**
- Django에서 계산한 밀집도의 2차 미분값
- 단위: `/min²` (분당 변화율의 변화량)
- 소수점 5자리까지 표시
- 가속도가 양수면 증가 속도가 빨라지는 중
- 가속도가 음수면 증가 속도가 둔화되거나 감소 속도가 빨라지는 중

**위험 도달 예상 시간 (ETA)**
- 현재 밀집도, 속도, 가속도를 기반으로 계산
- 2차 방정식 모델: `d + v*t + 0.5*a*t² = 임계값`
- 메시지 예시:
  - "약 8분 후 '위험' 진입 예상" (위험 임박)
  - "약 5분 후 '주의' 복귀 예상" (위험 해제 예상)
  - "현재 추세상 위험 단계 변동 징후 없음" (안정)
  - "추세 계산 표본 부족" (데이터 부족)

#### 2.3.4. 최근 알림 카드

**테이블 형식**
- 3개 컬럼: 시각, 유형, 메시지
- 최근 10건의 경보를 시간 역순으로 표시
- 각 행은 심각도에 따라 스타일링:
  - `alert-row--DANGER`: 빨간색 계열
  - `alert-row--WARNING`: 주황색 계열
  - `alert-row--INFO`: 파란색 계열

**데이터 로딩**
- API: `GET /api/v1/cameras/{cameraId}/alerts?limit=10`
- 페이지 로드 시 한 번 호출
- 응답: `List<StageAlertView>`
  - `timestamp`: 발생 시각
  - `title`: 경보 제목 (예: "위험 임박", "위험 수위 돌파")
  - `message`: 경보 메시지
  - `severity`: 심각도 (DANGER/WARNING/INFO)

**빈 상태**
- 경보가 없는 경우: "표시할 경보가 없습니다." 메시지
- 로딩 실패: "알림을 불러오는 데 실패했습니다." 오류 메시지

## 3. 데이터 흐름 및 로직

### 3.1. 초기 페이지 로드

1. **컨트롤러 처리** (`WebController.analysisPage()`)
   - `cameraId` 파라미터 확인
   - 모든 카메라 목록 조회 (`CameraService.fetchAll()`)
   - 선택된 카메라 결정:
     - `cameraId`가 있으면 해당 카메라 선택
     - 없으면 첫 번째 카메라 선택
   - 선택된 카메라의 최신 스냅샷 경로 조회
   - 선택된 카메라의 분석 요약 조회 (`AnalysisInsightsService.summarizeCamera()`)
   - 모든 데이터를 Model에 담아 템플릿으로 전달

2. **템플릿 렌더링**
   - Thymeleaf가 서버 데이터를 HTML로 렌더링
   - 카메라 선택 드롭다운 생성
   - 실시간 영상 iframe 생성
   - 최신 스냅샷 이미지 표시
   - 종합 분석 카드의 초기 데이터 표시

3. **JavaScript 초기화**
   - `DOMContentLoaded` 이벤트 발생
   - `data-selected-camera-id` 속성에서 카메라 ID 추출
   - 초기화 함수 `init()` 실행:
     - 차트 컨트롤 상태 업데이트
     - 시계열 차트 렌더링
     - 히트맵 데이터 페칭 및 렌더링
     - 최근 알림 페칭 및 렌더링
     - 통계적 이상 감지 페칭 및 렌더링
     - 비교 분석 페칭 및 렌더링
     - 히트맵 이벤트 리스너 설정

### 3.2. 시계열 차트 업데이트 흐름

1. **사용자 인터랙션**
   - 기간 버튼 클릭 또는 비교 버튼 클릭
   - 이벤트 핸들러가 `activePeriod` 또는 `activeCompare` 상태 변경
   - `updateControlStates()` 호출하여 버튼 활성화/비활성화
   - `updateChart()` 호출

2. **데이터 페칭** (`updateChart()`)
   - 플레이스홀더 표시, 차트 숨김
   - 현재 시각 기준 시작/종료 시각 계산
   - 주 데이터 API 호출
   - 비교 데이터 API 호출 (선택 시)
   - `Promise.all()`로 병렬 페칭

3. **데이터 처리**
   - 리샘플링: 불규칙한 데이터를 고정 시간 버킷으로 정규화
   - 비교 데이터 시간 이동: 타임스탬프를 주 데이터 시간대로 이동
   - 통계 계산: 각 버킷의 평균, 최소, 최대 계산
   - 레이블 생성: 시간 버킷에 대응하는 X축 레이블 생성

4. **차트 렌더링** (`renderChart()`)
   - 기존 차트 파괴 (있는 경우)
   - 4개 데이터셋 구성 (Range Min, Range Max, 평균, 비교)
   - Y축 스케일 계산
   - Chart.js 인스턴스 생성
   - 커스텀 툴팁 핸들러 설정
   - 플레이스홀더 숨김, 차트 표시

### 3.3. 히트맵 렌더링 흐름

1. **데이터 페칭** (`fetchAndRenderHeatmap()`)
   - API: `GET /api/v1/cameras/{cameraId}/congestion-heatmap`
   - 응답: 7개 요일 × 24시간 = 168개 데이터 포인트

2. **셀 색상 적용**
   - 각 요일 데이터를 순회
   - 각 시간대에 대해:
     - `data-day`와 `data-hour` 속성으로 해당 셀 선택
     - 최대 밀집도 값을 `getColorForDensity()` 함수로 색상 변환
     - 셀의 `backgroundColor` 스타일 적용
     - 툴팁 내용을 `data-tooltip-content` 속성에 저장

3. **툴팁 이벤트 설정** (`setupHeatmapEventListeners()`)
   - `mouseover`: 툴팁 표시, 내용 설정
   - `mouseout`: 툴팁 숨김
   - `mousemove`: 툴팁 위치를 커서 따라 이동

### 3.4. 통계적 이상 감지 흐름

1. **데이터 페칭** (`fetchAndRenderStatisticalAnomaly()`)
   - API: `GET /api/v1/cameras/{cameraId}/statistical-anomaly`
   - 응답: 분석 가능 여부, 통계 정보

2. **분석 불가 처리**
   - `isAnalyzable === false`인 경우:
     - 그래프 숨김
     - 상태 칩: 회색, 메시지 표시
     - 현재 밀집도만 표시

3. **시각화** (분석 가능한 경우)
   - 그래프 범위 계산: μ ± 3σ
   - 정상 범위 바 위치 계산: μ ± σ
   - 현재 값 마커 위치 계산
   - CSS로 위치 및 너비 설정
   - Z-점수에 따라 상태 칩 색상 변경
   - 이상 상태 시 마커에 `is-anomaly` 클래스 추가

## 4. 서비스 로직 상세

### 4.1. AnalysisInsightsService

#### 4.1.1. getCongestionHeatmapForLast7Days()
- 목적: 지난 7일간의 요일-시간대별 혼잡도 히트맵 데이터 생성
- 입력: `cameraId`
- 출력: `List<CongestionHeatmapPayload>`
- 동작:
  1. 7일 전부터 현재까지의 `READY` 상태 분석 로그 조회
  2. 로그를 요일별, 시간대별로 그룹화
  3. 각 그룹에 대해 `DoubleSummaryStatistics` 계산 (평균, 최대, 최소, 개수)
  4. 7개 요일 × 24시간 = 168개 데이터 포인트 생성
  5. 데이터가 없는 시간대는 0.0으로 채움

#### 4.1.2. getDensityHistory()
- 목적: 특정 기간의 밀집도 시계열 데이터 조회
- 입력: `cameraId`, `start`, `end`
- 출력: `List<DensityPointPayload>`
- 동작:
  1. 지정된 기간의 `READY` 상태 분석 로그를 시간 오름차순으로 조회
  2. 각 로그를 `DensityPointPayload`로 변환:
     - `timestamp`: 분석 시각
     - `density`: 밀집도
     - `personCount`: 탐지된 인원 수
     - `annotatedImagePath`: 분석 이미지 경로
  3. 리스트로 반환

#### 4.1.3. getStatisticalAnomaly()
- 목적: 현재 밀집도의 통계적 이상 여부 판단
- 입력: `cameraId`
- 출력: `StatisticalAnomalyPayload`
- 동작:
  1. 최신 `READY` 상태 분석 로그 조회
  2. 현재 요일 및 시간 추출
  3. 과거 4주간의 동일 요일, 동일 시간대(±1시간) 데이터 조회
  4. 최소 20개 데이터 포인트 필요, 부족하면 "분석 불가" 반환
  5. 평균(μ)과 표준편차(σ) 계산
  6. Z-점수 계산: `(현재값 - μ) / σ`
  7. Z-점수에 따라 메시지 결정:
     - `z > 2.5`: "이례적으로 높음"
     - `z > 1.5`: "평소보다 높음"
     - `z < -1.5`: "평소보다 낮음"
     - 그 외: "정상 범위"

#### 4.1.4. getComparisonSummary()
- 목적: 현재 밀집도를 어제/지난주 동시간과 비교
- 입력: `cameraId`
- 출력: `ComparisonSummaryPayload`
- 동작:
  1. 현재 시각 기준 ±7분 윈도우의 평균 밀집도 계산
  2. 24시간 전 동일 윈도우의 평균 밀집도 계산
  3. 7일 전 동일 윈도우의 평균 밀집도 계산
  4. 변화량 계산: `현재 - 과거` (단순 차이)
  5. 결과 반환:
     - `yesterdayDensity`: 어제 평균
     - `yesterdayChange`: 어제 대비 변화량
     - `lastWeekDensity`: 지난주 평균
     - `lastWeekChange`: 지난주 대비 변화량

## 5. 설계 의도 및 특징

### 5.1. 다층적 분석 접근
- **실시간 모니터링**: 라이브 스트림과 최신 스냅샷으로 현재 상황 파악
- **시계열 분석**: 차트를 통해 밀집도의 시간적 변화 추이 파악
- **패턴 분석**: 히트맵으로 요일/시간대별 반복 패턴 발견
- **이상 감지**: 통계적 방법으로 비정상적인 상황 조기 발견
- **예측 분석**: 추세 분석으로 미래 위험 예측

### 5.2. 인터랙티브 데이터 탐색
- 기간 및 비교 옵션을 자유롭게 선택하여 다양한 관점에서 데이터 분석
- 툴팁 고정 기능으로 특정 시점의 상세 정보를 편리하게 확인
- 히트맵 툴팁으로 각 시간대의 정확한 수치 확인

### 5.3. 시각적 정보 밀도 최적화
- 게이지, 차트, 히트맵 등 다양한 시각화 기법 활용
- 색상 코딩으로 직관적인 위험도 전달
- 범위 영역 표시로 변동성 정보 제공

### 5.4. 컨텍스트 제공
- 과거 데이터와의 비교로 현재 상황의 의미 파악
- 통계적 정상 범위 제시로 이상 여부 판단 기준 제공
- 스냅샷 이미지로 수치 뒤의 실제 상황 확인

### 5.5. 성능 최적화
- 병렬 API 호출로 로딩 시간 단축
- 데이터 리샘플링으로 차트 렌더링 성능 향상
- 커스텀 툴팁으로 Chart.js 기본 툴팁의 한계 극복

## 6. 주요 API 엔드포인트

| 엔드포인트 | 메서드 | 용도 | 호출 시점 |
|-----------|--------|------|----------|
| `/api/v1/cameras/{id}/density-history` | GET | 시계열 밀집도 데이터 조회 | 차트 업데이트 시 |
| `/api/v1/cameras/{id}/congestion-heatmap` | GET | 요일-시간대별 히트맵 데이터 조회 | 초기 1회 |
| `/api/v1/cameras/{id}/alerts?limit=10` | GET | 최근 경보 10건 조회 | 초기 1회 |
| `/api/v1/cameras/{id}/statistical-anomaly` | GET | 통계적 이상 감지 결과 조회 | 초기 1회 |
| `/api/v1/cameras/{id}/comparison-summary` | GET | 어제/지난주 대비 비교 데이터 조회 | 초기 1회 |

## 7. 데이터 모델

### 7.1. DensityPointPayload
```java
record DensityPointPayload(
    LocalDateTime timestamp,
    Double density,
    Integer personCount,
    String annotatedImagePath
)
```

### 7.2. CongestionHeatmapPayload
```java
record CongestionHeatmapPayload(
    String dayOfWeek,           // 요일 이름 (예: "월")
    int dayOfWeekIndex,         // 요일 인덱스 (1~7)
    List<Double> hourlyAverageDensities,  // 시간별 평균 (24개)
    List<Double> hourlyMaxDensities       // 시간별 최대 (24개)
)
```

### 7.3. StatisticalAnomalyPayload
```java
record StatisticalAnomalyPayload(
    boolean isAnalyzable,
    String message,
    Double currentDensity,
    Double averageDensity,
    Double stdDeviation,
    Double zScore
)
```

### 7.4. ComparisonSummaryPayload
```java
record ComparisonSummaryPayload(
    Double yesterdayDensity,
    Double yesterdayChange,
    Double lastWeekDensity,
    Double lastWeekChange
)
```
